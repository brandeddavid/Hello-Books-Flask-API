language: python

python:
    - 3.5.2
# install dependencies
install:
  - pip install -r requirements.txt
  - pip install coveralls

script:

  - coverage run modelstests.py

after_success:

  - coveralls